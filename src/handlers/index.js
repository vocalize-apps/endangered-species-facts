const launch = require('./launch');
const help = require('./help');
const end = require('./end');
const random_fact = require('./random_fact');
const species_fact = require('./species_fact');
const species_count = require('./species_count');
const yes_no = require('./yes_no');

module.exports = {
  launch,
  help,
  end,
  random_fact,
  species_fact,
  species_count,
  yes_no
};
